function theBeatlesplay (musicians, instruments) {
  //since musicians and instruments are included as parameter names, you can reference them inside the function definition to get access to the array that are passed in when the function is called
  }